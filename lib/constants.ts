/** "Go live in 48 hours" marketing promise — shared across the instant
 *  estimate (app/api/quote/calculate) and the approved-contract message
 *  (app/api/admin/checker) so the number can't drift between the two. */
export const DAYS_TO_DEPLOY_DEFAULT = 2;
